/**
 * CoinEconomy — اقتصاد عملات TiQ
 * العملة الوحيدة لشراء حزم الملصقات. تُكسب من كل الأوضاع:
 * سؤال اليوم · التدريب · السرعة · الترجيح · الكأس · المهام · بيع التكرارات.
 * كل المكافآت في مكان واحد لضمان توازن الاقتصاد وقابلية المعايرة.
 * ============================================================
 */

/**
 * دليل التوازن السريع: مصادر كسب العملة 🪙
 * سؤال يومي 10-25 · تدريب 3/هدف · سرعة 2/صحيحة +8 جولة · ترجيح محلي 4/هدف
 * كأس 6/هدف + 15..100/فوز + 250 لقب · مكررات 15..120 · ملصق جديد +10
 * أسعار الحزم: أساطير 300 · نجوم 200 · أندية 120 · منتخبات 120
 * كل رقم هنا نقطة معايرة واحدة.
 */

/** جدول المكافآت — كل أرقام الاقتصاد هنا (نقطة معايرة واحدة) */
export const COINS = {
  /** إجابة صحيحة على سؤال اليوم (حسب الصعوبة) */
  daily: { easy: 10, medium: 15, hard: 25 } as const,
  /** هدف صحيح في التدريب */
  trainGoal: 3,
  /** إجابة صحيحة في السرعة (الكومبو لا يؤثر — السعر ثابت لكل إجابة) */
  rushCorrect: 2,
  /** إكمال جولة سرعة كاملة */
  rushFinish: 8,
  /** هدف في الترجيح المحلي */
  penaltyGoal: 4,
  /** هدف في مباراة الكأس */
  cupGoal: 6,
  /** مكافأة الفوز بمباراة الكأس حسب الدور */
  cupWin: { r16: 15, qf: 25, sf: 40, final: 100 } as const,
  /** مكافأة الفوز بلقب الكأس */
  cupChampionBonus: 250,
  /** مكافأة المهام اليومية (تُضاف لـ XP) — streakGuard لا تمنح عملات */
  quest: { answerDaily: 20, trainMaster: 25, penaltyAce: 30, allBonus: 50 } as Partial<Record<string, number>> & { allBonus: number },
  /** مكافأة حفظ السلسلة اليومية (يوم/2 على الأقل 5) */
  streakBonus: 5,
  /** بيع ملصق مكرر حسب الندرة */
  sell: { common: 15, rare: 30, epic: 60, legendary: 120 } as const,
  /** مكافأة إكمال المجموعة الأولى في الألبوم (يُدفع مرة واحدة لكل ملصق جديد) */
  newSticker: 10,
} as const;

/** أسعار الحزم بالعملات — أرخص من XP لأن العملات مخصصة للشراء */
export const PACK_PRICES: Record<string, number> = {
  legends: 300,
  stars: 200,
  clubs: 120,
  nations: 120,
};

/** قيمة بيع ملصق مكرر حسب ندرة */
export function sellValue(rarity: "common" | "rare" | "epic" | "legendary"): number {
  return COINS.sell[rarity];
}

/** تنسيق رصيد العملات (فواصل الآلاف) */
export function formatCoins(n: number): string {
  return n.toLocaleString("en-US");
}
